#pragma once
#ifdef __has_include
// imspinner.h does not pull in the split headers, and this API declares widgets from all of them, so every header the
// implementation includes has to be checked here too. Keep this list in sync with the guard in cimspinner.cpp.
#if __has_include(<imspinner.h>) && __has_include(<imspinner_text.h>) && __has_include(<imspinner_bars.h>) && __has_include(<imspinner_dots.h>) && __has_include(<imspinner_shapes.h>) && __has_include(<imspinner_compat.h>)
#include "cimgui_extra_common.h"

#ifdef __cplusplus
extern "C"
{
#endif

#define IMSPINNER_PI 3.14159265358979323846f
// Note: this is 2 * pi, mirroring upstream imspinner's PI_2, not pi / 2 as the analogous <math.h> M_PI_2 would be.
#define IMSPINNER_PI_2 (IMSPINNER_PI * 2.f)

    // Returns the color of the leaf at the given index. Passed by value to ImSpinner_SpinnerCamera; it is already a
    // pointer type, so do not take its address.
    typedef CImXVec4(*ImSpinner_LeafColor)(int);

    MLS_PUBLIC_API void ImSpinner_SpinnerAngTriple(             const char* label, float        radius1,    float radius2,          float           radius3,        float           thickness,      const CImXVec4* c1,         const CImXVec4* c2,         const CImXVec4* c3,     float   speed,  float   angle);
    MLS_PUBLIC_API void ImSpinner_SpinnerAngTwin(               const char* label, float        radius1,    float radius2,          float           thickness,      const CImXVec4* color,          const CImXVec4* bg,         float           speed,      float           angle,  size_t  arcs,   int     mode );
    MLS_PUBLIC_API void ImSpinner_SpinnerTwinBall(              const char* label, float        radius1,    float radius2,          float           thickness,      float           b_thickness,    const CImXVec4* ball,       const CImXVec4* bg,         float           speed,  size_t  balls,  int     mode );
    MLS_PUBLIC_API void ImSpinner_SpinnerIngYang(               const char* label, float        radius,     float thickness,        bool            reverse,        float           yang_detlta_r,  const CImXVec4* colorI,     const CImXVec4* colorY,     float           speed,  float   angle,  int     mode );

    MLS_PUBLIC_API void ImSpinner_SpinnerTwinHboDots(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           minfade,        float           ryk,        float           speed,      size_t          dots,   float   delta   );
    MLS_PUBLIC_API void ImSpinner_SpinnerRainbow(               const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          float           ang_min,    float           ang_max,    int             arcs,   int     mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerRainbowMix(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          float           ang_min,    float           ang_max,    int             arcs,   int     mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerAng8(                  const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      float           angle,      int             mode,   float   rkoef   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDots(                  const char* label, float*       nextdot,    float radius,           float           thickness,      const CImXVec4* color,          float           speed,      size_t          dots,       float           minth,  int     mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerVDots(                 const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bgcolor,        float           speed,      size_t          dots,       size_t          mdots,  int     mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTwinAng(               const char* label, float        radius1,    float radius2,          float           thickness,      const CImXVec4* color1,         const CImXVec4* color2,     float           speed,      float           angle,  int     mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTwinAng180(            const char* label, float        radius1,    float radius2,          float           thickness,      const CImXVec4* color1,         const CImXVec4* color2,     float           speed,      float           angle,  int     mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTwinAng360(            const char* label, float        radius1,    float radius2,          float           thickness,      const CImXVec4* color1,         const CImXVec4* color2,     float           speed1,     float           speed2, int     mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerHboDots(               const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           minfade,        float           ryk,        float           speed,      size_t          dots,   int     mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerRainbowBalls(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             balls,      int             mode,       int             rings,  int     mx      );

    MLS_PUBLIC_API void ImSpinner_SpinnerCircleDrop(            const char* label, float        radius,     float thickness,        float           thickness_drop, const CImXVec4* color,          const CImXVec4* bg,         float           speed,      float           angle   );
    MLS_PUBLIC_API void ImSpinner_SpinnerAngMix(                const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          float           angle,      int             arcs,       int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerAng(                   const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      float           angle,      int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerPulsar(                const char* label, float        radius,     float thickness,        const CImXVec4* bg,             float           speed,          bool            sequence,   float           angle,      int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerIncScaleDots(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          dots,       float           angle,      int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerRotateSegments(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          arcs,       size_t          layers,     int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerPointsArcBounce(       const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          points,     int             circles,    float           rspeed  );
    MLS_PUBLIC_API void ImSpinner_SpinnerWifiIndicator(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      float           cangle,     int             dots    );
    MLS_PUBLIC_API void ImSpinner_SpinnerPatternEclipse(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             elipses,    float           delta_a,    float           delta_y );
    MLS_PUBLIC_API void ImSpinner_SpinnerThreeDotsStar(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           minfade,        float           ryk,        float           speed,      float           delta   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDnaDots(               const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             lt,         float           delta,      bool            mode    );
    MLS_PUBLIC_API void ImSpinner_Spinner3SmuggleDots(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             lt,         float           delta,      bool            mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerSplineAng(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      float           angle,      int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerPointsRoller(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          points,     int             circles,    float           rspeed  );
    MLS_PUBLIC_API void ImSpinner_SpinnerThickToSin(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             nt,         int             lt,         int             mode    );

    MLS_PUBLIC_API void ImSpinner_SpinnerTwinPulsar(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             rings,      int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerBounceDots(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          dots,       int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerFadeDots(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             lt,         int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerRotateDots(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             dots,       int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerRainbowCircle(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          arcs,       float           mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerRotateGooeyBalls(      const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             balls,      int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerBounceBall(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             dots,       bool            shadow      );
    MLS_PUBLIC_API void ImSpinner_SpinnerPulsarBall(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          bool            shadow,     int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerSomeScaleDots(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          dots,       int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerLoadingRing(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      int             segments    );
    MLS_PUBLIC_API void ImSpinner_SpinnerRotateShapes(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             shapes,     int             pnt         );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsToPoints(          const char* label, float        radius,     float thickness,        float           offset_k,       const CImXVec4* color,          float           speed,      size_t          dots        );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsToBar(             const char* label, float        radius,     float thickness,        float           offset_k,       const CImXVec4* color,          float           speed,      size_t          dots        );
    MLS_PUBLIC_API void ImSpinner_SpinnerTopup(                 const char* label, float        radius1,    float radius2,          const CImXVec4* color,          const CImXVec4* fg,             const CImXVec4* bg,         float           speed       );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsRotateFade(        const char* label, float        rmin,       float rmax,             float           thickness,      const CImXVec4* color,          float           speed,      size_t          bars        );
    MLS_PUBLIC_API void ImSpinner_SpinnerFilledArcRing(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      size_t          arcs        );
    MLS_PUBLIC_API void ImSpinner_SpinnerArcRotation(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          arcs,       int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerArcFade(               const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          arcs,       int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerRotatedAtom(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             elipses,    int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarChartSine(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             bars,       int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarChartRainbow(       const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             bars,       int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerSolarBalls(            const char* label, float        radius,     float thickness,        const CImXVec4* ball,           const CImXVec4* bg,             float           speed,      size_t          balls       );
    MLS_PUBLIC_API void ImSpinner_SpinnerSolarArcs(             const char* label, float        radius,     float thickness,        const CImXVec4* ball,           const CImXVec4* bg,             float           speed,      size_t          balls       );
    MLS_PUBLIC_API void ImSpinner_SpinnerTrianglesSelector(     const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      size_t          bars        );
    MLS_PUBLIC_API void ImSpinner_SpinnerRotateWheel(           const char* label, float        radius,     float thickness,        const CImXVec4* bg_color,       const CImXVec4* color,          float           speed,      size_t          pins        );
    MLS_PUBLIC_API void ImSpinner_SpinnerFlowingGradient(       const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      float           angle       );
    MLS_PUBLIC_API void ImSpinner_SpinnerMoonLine(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      float           angle       );
    MLS_PUBLIC_API void ImSpinner_SpinnerFluidPoints(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          dots,       float           delta       );
    MLS_PUBLIC_API void ImSpinner_SpinnerCamera(                const char* label, float        radius,     float thickness,        ImSpinner_LeafColor leaf_color, float           speed,          size_t          bars,       int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerTrianglesShift(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      size_t          bars        );
    MLS_PUBLIC_API void ImSpinner_SpinnerPointsShift(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed,      size_t          bars        );
    MLS_PUBLIC_API void ImSpinner_SpinnerCaleidoscope(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          arcs,       int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerModCircle(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           ang_min,        float           ang_max,    float           speed       );
    MLS_PUBLIC_API void ImSpinner_SpinnerRotateSegmentsPulsar(  const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          arcs,       size_t          layers      );
    MLS_PUBLIC_API void ImSpinner_SpinnerFadeTris(              const char* label, float        radius,     const CImXVec4* color,  float           speed,          size_t          dim,            bool            scale,      int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerRotateTriangles(       const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             tris,       int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerRainbowShot(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             balls,      int             mode        );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextUnderlineDots(     const char* label, float        radius,     const CImXVec4* color,  float           speed,          float           thickness,      int             dashes,     const char*     text        );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextColorFill(         const char* label, float        radius,     const CImXVec4* color,  const CImXVec4* bg,             float           speed,          int             mode,       const char*     text        );

    MLS_PUBLIC_API void ImSpinner_SpinnerDotRing(               const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerConicWheels(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerOrbitMoon(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerArcArrow(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerConicGrid(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextScanline(          const char* label, float        radius,     const CImXVec4* color,  float           speed,          int             mode,           const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextTumble(            const char* label, float        radius,     const CImXVec4* color,  float           speed,          int             mode,           const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextShake(             const char* label, float        radius,     const CImXVec4* color,  float           speed,          int             mode,           const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextScrollSweep(       const char* label, float        radius,     const CImXVec4* color,  const CImXVec4* bg,             float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextShine(             const char* label, float        radius,     const CImXVec4* color,  const CImXVec4* bg,             float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextSweep(             const char* label, float        radius,     const CImXVec4* color,  const CImXVec4* bg,             float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextUnderlineScroll(   const char* label, float        radius,     const CImXVec4* color,  float           speed,          float           thickness,      const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextSplit(             const char* label, float        radius,     const CImXVec4* color,  float           speed,          int             mode,           const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextTyping(            const char* label, float        radius,     const CImXVec4* color,  float           speed,          int             tail,           const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextUnderline(         const char* label, float        radius,     const CImXVec4* color,  float           speed,          float           thickness,      const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerRotatingHeart(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          float           ang_min );
    MLS_PUBLIC_API void ImSpinner_SpinnerClock(                 const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerFilling(               const char* label, float        radius,     float thickness,        const CImXVec4* color1,         const CImXVec4* color2,         float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerFillingMem(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          CImXVec4*       colorbg,        float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerAsciiSymbolPoints(     const char* label, const char*  text,       float radius,           float           thickness,      const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextFading(            const char* label, const char*  text,       float radius,           float           fsize,          const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSevenSegments(         const char* label, const char*  text,       float radius,           float           thickness,      const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSinSquares(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerSurroundedIndicator(   const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerLemniscate(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          float           angle   );
    MLS_PUBLIC_API void ImSpinner_SpinnerRotateGear(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          pins    );
    MLS_PUBLIC_API void ImSpinner_SpinnerAtom(                  const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             elipses );
    MLS_PUBLIC_API void ImSpinner_SpinnerPatternRings(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             elipses );
    MLS_PUBLIC_API void ImSpinner_SpinnerPatternSphere(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             elipses );
    MLS_PUBLIC_API void ImSpinner_SpinnerRingSynchronous(       const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             elipses );
    MLS_PUBLIC_API void ImSpinner_SpinnerRingWatermarks(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             elipses );
    MLS_PUBLIC_API void ImSpinner_SpinnerSpiral(                const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          arcs    );
    MLS_PUBLIC_API void ImSpinner_SpinnerFadePulsar(            const char* label, float        radius,     const CImXVec4* color,  float           speed,          int             rings,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerCircularLines(         const char* label, float        radius,     const CImXVec4* color,  float           speed,          int             lines,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerFilledArcFade(         const char* label, float        radius,     const CImXVec4* color,  float           speed,          size_t          arcs,           int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerZipDots(               const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          dots    );
    MLS_PUBLIC_API void ImSpinner_SpinnerWaveDots(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             lt      );
    MLS_PUBLIC_API void ImSpinner_SpinnerThreeDots(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             lt      );
    MLS_PUBLIC_API void ImSpinner_SpinnerFiveDots(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             lt      );
    MLS_PUBLIC_API void ImSpinner_Spinner4Caleidospcope(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             lt      );
    MLS_PUBLIC_API void ImSpinner_SpinnerMultiFadeDots(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             lt      );
    MLS_PUBLIC_API void ImSpinner_SpinnerScaleDots(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             lt      );
    MLS_PUBLIC_API void ImSpinner_SpinnerMovingDots(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          dots    );
    MLS_PUBLIC_API void ImSpinner_SpinnerOrionDots(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             arcs    );
    MLS_PUBLIC_API void ImSpinner_SpinnerGalaxyDots(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             arcs    );
    MLS_PUBLIC_API void ImSpinner_SpinnerIncDots(               const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          dots    );
    MLS_PUBLIC_API void ImSpinner_SpinnerIncFullDots(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          dots    );
    MLS_PUBLIC_API void ImSpinner_SpinnerFadeBars(              const char* label, float        w,          const CImXVec4* color,  float           speed,          size_t          bars,           bool            scale   );
    MLS_PUBLIC_API void ImSpinner_SpinnerFilledArcColor(        const char* label, float        radius,     const CImXVec4* color,  const CImXVec4* bg,             float           speed,          size_t          arcs    );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsLoading(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          const CImXVec4* bg,             float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerHerbertBalls(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             balls   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSolarScaleBalls(       const char* label, float        radius,     float thickness,        const CImXVec4* ball,           float           speed,          size_t          balls   );
    MLS_PUBLIC_API void ImSpinner_SpinnerMovingArcs(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          arcs    );
    MLS_PUBLIC_API void ImSpinner_SpinnerAngEclipse(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          float           angle   );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarChartAdvSine(       const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarChartAdvSineFade(   const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBlocks(                const char* label, float        radius,     float thickness,        const CImXVec4* bg,             const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerTwinBlocks(            const char* label, float        radius,     float thickness,        const CImXVec4* bg,             const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSquareRandomDots(      const char* label, float        radius,     float thickness,        const CImXVec4* bg,             const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerScaleBlocks(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerMoonDots(              const char* label, float        radius,     float thickness,        const CImXVec4* first,          const CImXVec4* second,         float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerArcPolarFade(          const char* label, float        radius,     const CImXVec4* color,  float           speed,          size_t          arcs,           int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerCircularPoints(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             lines   );
    MLS_PUBLIC_API void ImSpinner_SpinnerCurvedCircle(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          size_t          circles );
    MLS_PUBLIC_API void ImSpinner_SpinnerFadePulsarSquare(      const char* label, float        radius,     const CImXVec4* color,  float           speed,          int             rings,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerArcWedges(             const char* label, float        radius,     const CImXVec4* color,  float           speed,          size_t          arcs,           int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerArcPolarRadius(        const char* label, float        radius,     const CImXVec4* color,  float           speed,          size_t          arcs,           int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerMorphShape(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerFlipTriangle(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerFoldSquare(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerPinwheel(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerCornerSquares(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerSplitSquare(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerCornerBurst(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerSkewSquare(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTumbleSquare(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTriDiamond(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTwinBlades(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerCrossBlades(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerDriftSquare(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerPieBounce(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerLeapFrog(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerPlusSquares(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerSevenMarch(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerCornerArcs(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerWalkBlocks(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTumbleHalfDisc(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerFoldHalfDisc(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerWiperArrow(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerSkewSquares(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerStretchSquares(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTriPie(                const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerShuffleBars(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerHingeTumble(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerDiagonalFlip(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerPulseGrid(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerMarchGrid(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerSpinBars(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerMorphDots(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerCombDiscs(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerOrbitDisc(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsTyping(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             dots    );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsStep(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             dots    );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsGather(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             dots    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsSeqPulse(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsCascadeGrow(       const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsRise(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsCornerHop(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsDiagonalGrow(      const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsBounceCenter(      const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsScaleAlt(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsCornerWave(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsJump(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsDoubleRow(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsSixPulse(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsSixStagger(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsMorphPlus(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsFlipSix(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsSwapTriBottom(     const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsSwapTriZigzag(     const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsMorphDiagonal(     const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsSlideDiagonal(     const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsConicAlternate(    const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsConicWalk(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsMarchIn(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsMarchDown(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsWaveSkew(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsRowDrop(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsZigzagRows(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsFillCascade(       const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsPingPongRows(      const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsWaveRowsWide(      const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsGridFade(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsGridSpread(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsStretch(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsStretchSeq(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsBounceBall(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsKnockDots(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsStaircase(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsKnockAway(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsGates(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsCapture(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsEscape(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsDevour(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsLift(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsGapSlide(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsConverge(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsSwapEnds(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsRelay(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsPush(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsPushWave(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsGather(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsSplit(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsSlot(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed,          int             mode    );

    MLS_PUBLIC_API void ImSpinner_SpinnerTextVanish(            const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextDecode(            const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextSquash(            const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextDropout(           const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextBlur(              const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextVibrate(           const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextRollWave(          const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextSwirl(             const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextSpin(              const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextFlip(              const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextSpotlight(         const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextWave(              const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextReveal(            const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextConveyor(          const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextCascade(           const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextColorful(          const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextRoll(              const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextBounce(            const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextColorCycle(        const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextScrollColors(      const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextScroll(            const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerTextFade(              const char* label, float        radius,     const CImXVec4* color,  float           speed,          const char*     text    );
    MLS_PUBLIC_API void ImSpinner_SpinnerGooeyBalls(            const char* label, float        radius,     const CImXVec4* color,  float           speed,          int             mode    );
    // thickness is accepted for signature compatibility but ignored by upstream imspinner.
    MLS_PUBLIC_API void ImSpinner_SpinnerDoubleFadePulsar(      const char* label, float        radius,     float thickness,        const CImXVec4* bg,             float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerBarsScaleMiddle(       const char* label, float        w,          const CImXVec4* color,  float           speed,          size_t          bars    );
    MLS_PUBLIC_API void ImSpinner_SpinnerHerbertBalls3D(        const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSimpleArcFade(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSquareStrokeFade(      const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSquareStrokeFill(      const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSquareStrokeLoading(   const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSquareLoading(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSquareSpins(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSpiralEye(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerScaleSquares(          const char* label, float        radius,     float thikness,         const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerFluid(                 const char* label, float        radius,     const CImXVec4* color,  float           speed,          int             bars    );
    MLS_PUBLIC_API void ImSpinner_SpinnerSineArcs(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerSwingDots(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsShift(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsOrbit(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsCircle(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsSquare(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsShuffle(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsSplit(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsLeader(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsRolling(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsTriangle(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsCascade(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsSwap(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsSpread(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsTwin(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsHop(               const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsJiggle(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsVibrate(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsWiper(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsCollapse(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsFlip(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsTurn(              const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsCarousel(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsHalfTurn(          const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsSlideFlip(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsStaggerTurn(       const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsStretch(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsMirrorStretch(     const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsPinch(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsCorners(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsNudgeRotate(       const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsUnfold(            const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsShuttle(           const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsSpreadShuttle(     const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsTriad(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsSatellite(         const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );
    MLS_PUBLIC_API void ImSpinner_SpinnerDotsTrack(             const char* label, float        radius,     float thickness,        const CImXVec4* color,          float           speed   );

    MLS_PUBLIC_API void ImSpinner_SpinnerTextScramble(          const char* label, float        radius,     const CImXVec4* color,  float           speed);
    MLS_PUBLIC_API void ImSpinner_SpinnerTextGlitch(            const char* label, float        radius,     float           speed,  const char*     text);
    MLS_PUBLIC_API void ImSpinner_SpinnerSquishSquare(          const char* label, float        radius,     const CImXVec4* color,  float           speed);
#ifdef __cplusplus
}
#endif
#endif
#endif